class Solution:
    def isPalindrome(self, x: int) -> bool:
        rev_num = 0
        num = x
         
        while num > 0:
            rem = num % 10
            rev_num = (rev_num * 10) + rem
            num = num // 10

        if x == rev_num:
            return True
        else:
            return False
